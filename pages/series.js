import Layout from '../components/layout';

const Series = () => {
    return (
    <Layout>
      <div>
        <div>Buraya serisler</div>
        {/* <Content
          entries={this.props.movies}
          isReady={this.props.isReady}
          isSomethingWrong={this.props.isSomethingWrong}
        /> */}
      </div>
    </Layout>
    );
  }

export default Series;
